# 합성곱 신경망을 사용한 이미지 분류

## 패션 MNIST 데이터 불러오기

```python
# 데이터
from tensorflow import keras
from sklearn.mode_selection import train_test_split
(train_input, train_target), (test_input, test_target)=\
  keras.datasets.fashion_mnist.load_data()
train_scaled=train_input.reshape(-1,28,28,1)/255.0
train_scaled, val_scaled, train_target, val_target = train_test_split(
    train_scaled, train_target, test_size=0.2, random_state=42
)
```

## 합성곱 신경망 만들기

```python
# 합성곱층
model=keras.Sequential()
model.add(keras.layers.Conv2D(32,kernel_size=3, activation='relu',
          padding='same',input_shape=(28,28,1)))
# maxpooling
model.add(keras.layers.MaxPooling2D(2))
#합성곱층2
model.add(keras.layers.Conv2D(64,kernel_size=3, activation='relu',
          padding='same'))
model.add(keras.layers.MaxPooling2D(2))
# 1차원 변환 및 밀집 은닉층 추가
model.add(keras.layers.Flatten())
model.add(keras.layers.Dense(100,activation='relu'))
model.add(keras.layers.Dropout(0.4))
model.add(keras.layers.Dense(10,activation='softmax'))
```

<img width="736" height="695" alt="image" src="https://github.com/user-attachments/assets/dd59d085-c938-49b4-9833-a738d83229b1" />

## 모델 컴파일과 훈련

```python
# 컴파일 ,훈련
model.compile(optimizer='adam',loss='sparse_categorical_crossentropy',
              metrics=['accuracy'])
checkpoint_cb=keras.callbacks.ModelCheckpoint('best-model.h5',save_best_only=True)
checkpoint_cb=keras.callbacks.EarlyStopping(patience=2,
                                            restore_best_weights=True)
history=model.fit(train_scaled,train_target, epochs=20,
                  validation_data=(val_scaled, val_target).
                  callbacks=[checkpoint_cb,early_stopping_cb])
# 평가
model.evalute(val_scaled,val_target)
# 첫 번째 샘플
plt.imshow(val_Scaled[0].reshape(28,28),cmap='gray_r')
plt.show()
# 예측 확률
preds = model.predict(val_scaled[0:1])
print(preds)
# 막대 그래프
plt.bar(range(1,11),preds[0])
plt.xlabel('class')
plt.ylabel('prob.')
plt.show()
# 레이블
classes=['티셔츠','바지','스웨터','드레스','코트','샌달','셔츠','스니커즈',
         '가방','앵클 부츠']
# 가장 큰 인덱스
import numpy as np
print(classes[np.argmax(preds)])
# 0-1 변환
test_scaled=test_input.reshape(-1,28,28,1)/255.0
# 테스트 세트 성능 측정
model.evaluate(test_scaled,test_target)
```

