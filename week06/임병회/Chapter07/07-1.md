# 인공 신경망

## 패션 MNIST

MNIST - 딥러닝을 처음 배울 때 많이 사용하는 데이터셋<br>


```python
# MNIST 데이터 다운
from tensorflow import keras
(train_input,train_target), (test_input,test_target)=\
  keras.datasets.fashion_mnist.load_data()
# 그림 출력
import matplotlib.pyplot as plt
fig, axs = plt.subplots(1, 10, figsize=(10, 10))
for i in range(10):
  axs[i].imshow(train_input[i], cmap='gray_r')
  axs[i].axis('off')
plt.show()
```

<img width="633" height="73" alt="image" src="https://github.com/user-attachments/assets/54d2defd-d0ea-4c72-8b09-0340fd77bb5e" />

```python
# 레이블 당 샘플 개수
import numpy as np
print(np.unique(train_target, return_counts=True))
```

<img width="713" height="75" alt="image" src="https://github.com/user-attachments/assets/bf2acd69-82d5-40ca-bf6f-cf0e40cfa5dd" />

## 아이템 분류 by lr

로지스틱 회귀공식<br>

<img width="636" height="72" alt="image" src="https://github.com/user-attachments/assets/611e1de8-600e-4eb6-a73a-97739f9b4076" />

MNIST에 적용 시<br>

<img width="625" height="42" alt="image" src="https://github.com/user-attachments/assets/81a90a6c-90f2-44af-ae6a-ab44075bfcfe" />

<img width="452" height="321" alt="image" src="https://github.com/user-attachments/assets/93375dd0-5cb4-46f3-b2b6-0dcd75b6c061" />

## 인공 신경망

<img width="549" height="409" alt="image" src="https://github.com/user-attachments/assets/278636a7-1885-47c0-996b-638163590b7c" />

### 텐서플로와 케라스

텐서플로 - 구글이 오픈소스로 공개한 딥러닝 라이브러리<br>
케라스 - 텐서플로의 고수준 API, 사실상 텐서플로와 동의어<br>

```python
# 텐서플로
import tensorflow as tf
# 케라스
from tensorflow import keras
```

## 인공신경망으로 모델 만들기

밀집층(완전연결층)<br>

<img width="386" height="396" alt="image" src="https://github.com/user-attachments/assets/b2f56551-d9be-4806-8021-0b8bb650a6d7" />

``` python
# 밀집층
dense = keras.layers.Dense(10, activation='softmax', input_shape(784,))
# 신경망 모델
model = keras.Sequential([dense])
```

<img width="524" height="370" alt="image" src="https://github.com/user-attachments/assets/037a4b43-d3da-42e4-bbf8-8307ab9b8f66" />

활성화함수 - 소프트맥스와 같이 뉴런의 선형 방정식 계산 결과에 적용되는 함수<br>

## 분류 by 인공 신경망

```python
# 컴파일
model.compile(loss='sparse_categorical_crossentropy', metrics=['accuracy'])
# 훈련
model.fit(train_scaled, train_target, epochs=5)
# 성능 평가
model.evaluate(val_scaled, val_target)
```

원-핫 인코딩<br>

<img width="484" height="334" alt="image" src="https://github.com/user-attachments/assets/2c0aee0c-1b81-462b-87c0-78d452857696" />

텐서플로에서는 정수로 된 타깃값을 원-핫 인코딩으로 변환 안해도 사용 가능 by 'sparse_categorical_crossentropy'<br>
